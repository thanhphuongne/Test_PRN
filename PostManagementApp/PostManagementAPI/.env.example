# Database Configuration
# Use either format below (both work)
# Format 1: PostgreSQL URL (recommended for Render/Heroku)
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_ID:YOUR_PASSWORD@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres

# Format 2: Connection string components
ConnectionStrings__DefaultConnection=Host=aws-1-ap-southeast-1.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.YOUR_PROJECT_ID;Password=YOUR_PASSWORD;SSL Mode=Require

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production

# Server URLs (Render uses PORT environment variable)
ASPNETCORE_URLS=http://+:8080

# CORS Settings (Optional - currently allows all origins)
# If you want to restrict CORS, you'll need to modify Program.cs
AllowedOrigins=https://test-prn.vercel.app

# Logging Level (Optional)
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning
